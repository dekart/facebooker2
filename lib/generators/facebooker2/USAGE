Description:
    Generate config file, initializer and add required 'include' line into ApplicationController
    Use 'canvas' parameter if you plan to create canvas application.

Example:
    rails generate facebooker2
    rails generate facebooker2 regular
    rails generate facebooker2 canvas
